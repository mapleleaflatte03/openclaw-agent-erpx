apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
spec:
  template:
    spec:
      containers:
        - name: agent-service
          envFrom:
            - configMapRef:
                name: agent-config
            - secretRef:
                name: agent-secrets
            - secretRef:
                name: agent-llm

