# BÁO CÁO QA — Accounting Agent Layer ERP AI Kế toán

**Ngày kiểm tra:** 2026-02-10  
**Người kiểm tra:** QA Agent (Blackbox — không sửa code, không đổi config)  
**Phiên bản:** commit `ce08633` trên nhánh `main`  
**Môi trường:** k3s staging (`accounting-agent-staging`), API nội bộ `http://127.0.0.1:30080`  
**Web UI:** https://app.welliam.codes/  

---

## TỔNG QUAN

| Hạng mục | Kết quả |
|----------|---------|
| Tổng test case | 7 nhóm kiểm tra |
| PASS | 5/7 |
| CONDITIONAL PASS | 1/7 |
| FAIL | 1/7 |
| Bug phát hiện | 4 (2× P1, 1× P2, 1× P3) |
| Regression | Không phát hiện regression |

---

## TEST 1: Health + Regression — ✅ PASS

**Mục tiêu:** Xác nhận tất cả endpoint health/readiness hoạt động, dữ liệu mock có sẵn.

| Endpoint | HTTP | Kết quả |
|----------|------|---------|
| `/healthz` | 200 | `{"status":"ok"}` |
| `/readyz` | 200 | `{"status":"ready"}` |
| `/agent/v1/healthz` | 200 | `{"status":"ok"}` |
| `/agent/v1/readyz` | 200 | `{"status":"ready"}` |
| `/agent/v1/acct/vouchers` | 200 | 3+ items (PT-000027, 635,000 VND) |
| `/agent/v1/acct/journal_proposals` | 200 | 3 items (approved/pending) |
| `/agent/v1/acct/anomaly_flags` | 200 | 3 items (severity=high/medium) |
| `/agent/v1/acct/cashflow_forecast` | 200 | 3 items (inflow/outflow) |
| `/agent/v1/runs` | 200 | 10+ items (attachment, contract_obligation) |
| `/agent/v1/contract/cases` | 200 | 3 items (status=open) |

**Web UI:** 11 tab hiển thị đầy đủ, timeline hoạt động bình thường, không có lỗi/traceback.

**Kết luận:** Tất cả endpoint hoạt động ổn định. Không phát hiện regression từ phiên bản trước.

---

## TEST 2: Q&A Nghiệp vụ Kế toán — ❌ FAIL

**Mục tiêu:** Kiểm tra chất lượng trả lời câu hỏi kế toán phức tạp qua endpoint `/agent/v1/acct/qna`.

### Câu hỏi #1: So sánh TK 131 vs TK 331
- **Input:** "So sánh tài khoản 131 (Phải thu khách hàng) và 331 (Phải trả người bán) theo TT133..."
- **HTTP:** 200
- **`llm_used`:** `false` ← **BUG**
- **`used_models`:** `[]`
- **Kết quả:** Trả về danh sách quy định chung (rule-based fallback), KHÔNG trả lời câu hỏi so sánh.
- **English CoT leak:** Không (0 từ tiếng Anh) ✅

### Câu hỏi #2: So sánh TK 642 vs TK 641 kèm ví dụ
- **Input:** "Phân biệt chi phí quản lý doanh nghiệp (TK 642) vs chi phí bán hàng (TK 641)..."
- **HTTP:** 200
- **`llm_used`:** `true`
- **`used_models`:** `["OpenAI GPT-oss-120b"]`
- **Kết quả:** LLM được gọi nhưng trả lời rỗng: *"Xin lỗi, hệ thống chưa thể đưa ra câu trả lời rõ ràng."* ← **BUG**
- **English CoT leak:** Không ✅

### Câu hỏi #3: Hạch toán khấu hao TSCĐ
- **Input:** "Hạch toán khấu hao tài sản cố định (TK 214) theo phương pháp đường thẳng..."
- **HTTP:** 200
- **`llm_used`:** `false` ← **BUG**
- **`used_models`:** `[]`
- **Kết quả:** Rule-based fallback yêu cầu số chứng từ cụ thể, KHÔNG trả lời câu hỏi.
- **English CoT leak:** Không ✅

**Điểm tích cực:** Fix DEV-04 (chống rò rỉ CoT tiếng Anh) hoạt động — 0/3 câu trả lời có từ tiếng Anh.

**Kết luận:** QnA endpoint KHÔNG đạt yêu cầu. 2/3 câu hỏi không kích hoạt LLM, 1/3 kích hoạt nhưng trả rỗng. Người dùng cuối không nhận được trợ giúp kế toán hữu ích.

---

## TEST 3: Validation Run + Period Check — ✅ PASS

**Mục tiêu:** Kiểm tra validation period khi tạo run.

| Test case | Payload | HTTP | Kết quả |
|-----------|---------|------|---------|
| Thiếu period | `{"run_type":"voucher_ingest","payload":{}}` | **422** | `"period là bắt buộc cho run_type=voucher_ingest, định dạng YYYY-MM"` ✅ |
| Period sai (2026-13) | `{"run_type":"soft_checks","payload":{"period":"2026-13"}}` | **422** | `"period '2026-13' không hợp lệ. Định dạng đúng: YYYY-MM"` ✅ |
| Period hợp lệ (2026-01) | `{"run_type":"voucher_ingest","payload":{"period":"2026-01"}}` | **200** | `run_id=3c1276d9-...`, `status=success` ✅ |

**Ghi nhận thêm:**
- Run mới tạo (voucher_ingest, 2026-01) xuất hiện trong danh sách `/agent/v1/runs` ✅
- Thông báo lỗi bằng tiếng Việt, kèm ví dụ định dạng ✅

**Kết luận:** Period validation hoạt động đúng theo thiết kế. Thông báo lỗi rõ ràng, bằng tiếng Việt.

---

## TEST 4: Validation Issues Drilldown — ✅ PASS

**Mục tiêu:** Kiểm tra drilldown từ soft_check_results → validation_issues theo check_result_id.

| Bước | Endpoint | HTTP | Kết quả |
|------|----------|------|---------|
| Lấy soft_check_results | `GET /acct/soft_check_results?limit=5` | 200 | 5 items, period=2026-01/02, score=0.9615 |
| Drilldown bằng ID | `GET /acct/validation_issues?check_result_id=89876bee-...` | 200 | 5 issues — tất cả cùng check_result_id ✅ |

**Chi tiết drilldown:**
- 3× `MISSING_ATTACHMENT` (severity=warning): PT-000010, PT-000020, PT-000030
- 2× `JOURNAL_IMBALANCED` (severity=error): JRN-0007 (Nợ=107,000 ≠ Có=107,123), JRN-0014 (Nợ=114,000 ≠ Có=114,123)
- Tổng (3 warnings + 2 errors) khớp với parent record (warnings=3, errors=2) ✅

**Kết luận:** Drilldown hoạt động chính xác. Dữ liệu nhất quán giữa parent và child.

---

## TEST 5: Diagnostics LLM Masking — ✅ PASS

**Mục tiêu:** Xác nhận endpoint `/diagnostics/llm` che giấu thông tin nhạy cảm.

```json
{
  "status": "ok",
  "do_agent": {
    "base_url_masked": "configured",
    "model_name": "OpenAI GPT-oss-120b",
    "health": "ok",
    "latency_ms": {"health": 825, "chat": 1608, "total": 2433},
    "response": {"id": "cmpl-1770738628", "model": "openai-gpt-oss-120b", "content_preview": "{\"ok\": true}"}
  }
}
```

| Tiêu chí | Kết quả |
|----------|---------|
| Không chứa raw URL (http/https) | ✅ Không phát hiện URL nào |
| Có `base_url_masked = "configured"` | ✅ |
| Có `model_name` | ✅ OpenAI GPT-oss-120b |
| Có `health` status | ✅ "ok" |
| Có `latency_ms` | ✅ health=825ms, chat=1608ms |

**Kết luận:** Fix DEV-01 (masking LLM base_url) hoạt động đúng. Không rò rỉ thông tin nhạy cảm.

---

## TEST 6: VN Feeder + Command Center — ⚠️ CONDITIONAL PASS

**Mục tiêu:** Kiểm tra VN Data Stream và Command Center tab mới.

### 6a. VN Data Catalog (code review)
- **3 nguồn Kaggle:** MC-OCR 2021, Receipt OCR, Appen VN OCR ✅
- **Unified schema:** `VnInvoiceRecord` dataclass với `source_name`, `seller_name`, `total_amount`, etc. ✅

### 6b. API Endpoints

| Endpoint | HTTP | Kết quả |
|----------|------|---------|
| `GET /vn_feeder/status` | 200 | `running: false`, `total_events_today: 0` |
| `POST /vn_feeder/control {"action":"start"}` | 200 | `{"status":"ok","action":"start"}` |
| `POST /vn_feeder/control {"action":"invalid_xyz"}` | 422 | Đúng: chỉ cho phép `start\|stop\|inject_now` |
| `GET /vn_feeder/status` (sau start) | 200 | Vẫn `running: false` ← Feeder process chưa chạy |

### 6c. Command Center UI Tab
- Tab "🎛️ Command Center (VN Agent)" hiển thị trong UI ✅
- 11/11 tab visible trên web ✅

### 6d. Phát hiện
- **Feeder process không chạy:** POST start trả 200 OK nhưng status vẫn `running: false`. Nguyên nhân: feeder subprocess (`vn_invoice_feeder.py`) chưa được khởi chạy trong pod k3s. API endpoint chỉ là control proxy.
- **Không có events:** `total_events_today: 0`, `sources: []`, `last_event_at: ""`

**Kết luận:** CONDITIONAL PASS — API endpoints hoạt động đúng về mặt HTTP/validation, nhưng feeder process thực tế chưa chạy nên không có dữ liệu VN invoice stream. Cần deploy feeder subprocess hoặc sidecar container.

---

## TEST 7: End-to-End ERP Chain — ✅ PASS

**Mục tiêu:** Xác nhận chuỗi dữ liệu ERP từ chứng từ → bút toán → kiểm tra → báo cáo.

### 7a. Chuỗi dữ liệu chính

| Bước | Endpoint | Items | Ghi chú |
|------|----------|-------|---------|
| Chứng từ | `/acct/vouchers` | 3+ | PT-000027..029, VND, mô tả tiếng Việt ✅ |
| Bút toán đề xuất | `/acct/journal_proposals` | 3 | approved/pending, tiếng Việt ✅ |
| Giao dịch bất thường | `/acct/anomaly_flags` | 3 | high/medium severity, bank tx refs ✅ |
| Dòng tiền | `/acct/cashflow_forecast` | 3 | direction=inflow, amount=1,030,000 VND ✅ |
| Giao dịch ngân hàng | `/acct/bank_transactions` | 3 | bank_tx_ref, counterparty, memo ✅ |
| Phân loại chứng từ | `/acct/voucher_classification_stats` | 1 | 33 UNCLASSIFIED (mock data) ✅ |
| QnA Audits | `/acct/qna_audits` | 3 | question/answer/sources/feedback ✅ |
| Kiểm tra mềm | `/acct/soft_check_results` | 5 | score=0.9615, period=2026-02 ✅ |
| Report Snapshots | `/acct/report_snapshots` | 0 | Chưa có snapshot — hợp lý cho env mới |

### 7b. Agent Core

| Endpoint | HTTP | Kết quả |
|----------|------|---------|
| `/agent/goals` | 200 | 10 goal chains tiếng Việt (đóng sổ, kiểm tra kỳ, đối chiếu...) ✅ |
| `/agent/timeline` | 200 | Events (run/task) với icon, title tiếng Việt ✅ |
| `/agent/commands` (GET) | 405 | Method Not Allowed — endpoint có thể chỉ hỗ trợ POST |
| `/contract/cases` | 200 | 3 cases, status=open ✅ |

### 7c. Bảo mật

| Kiểm tra | Kết quả |
|----------|---------|
| Truy cập không có API key | HTTP 401 ✅ |
| Diagnostics không lộ URL | ✅ (đã kiểm tra Test 5) |
| QnA không lộ CoT tiếng Anh | ✅ (đã kiểm tra Test 2) |

**Kết luận:** Chuỗi ERP hoạt động end-to-end. Dữ liệu nhất quán, tiếng Việt toàn bộ, bảo mật đúng.

---

## DANH SÁCH BUG

### BUG-QNA-01 — P1 (Cao)
- **Mô tả:** LLM path không được kích hoạt cho các câu hỏi kế toán phức tạp
- **Tái hiện:** POST `/acct/qna` với câu hỏi so sánh TK (131 vs 331, khấu hao TSCĐ)
- **Hiện tượng:** `llm_used: false`, `used_models: []`, trả về rule-based fallback không hữu ích
- **Tần suất:** 2/3 câu hỏi test (66%)
- **Tác động:** Người dùng không nhận được câu trả lời kế toán chất lượng

### BUG-QNA-02 — P1 (Cao)
- **Mô tả:** Khi LLM được kích hoạt, trả về câu trả lời rỗng/thất bại
- **Tái hiện:** POST `/acct/qna` với câu hỏi TK 642 vs 641
- **Hiện tượng:** `llm_used: true`, `used_models: ["OpenAI GPT-oss-120b"]`, nhưng answer = "Xin lỗi, hệ thống chưa thể đưa ra câu trả lời rõ ràng."
- **Tần suất:** 1/3 câu hỏi test (33%)
- **Tác động:** LLM gọi thành công nhưng không trích xuất được câu trả lời hữu ích

### BUG-FEEDER-01 — P2 (Trung bình)
- **Mô tả:** VN Feeder control endpoint trả 200 OK cho "start" nhưng feeder process không thực sự chạy
- **Tái hiện:** POST `/vn_feeder/control {"action":"start"}` → 200 OK, sau đó GET `/vn_feeder/status` → `running: false`
- **Nguyên nhân có thể:** Feeder subprocess chưa được deploy trong pod k3s
- **Tác động:** Command Center không có dữ liệu VN invoice, người dùng thấy "Đã dừng"

### BUG-API-01 — P3 (Thấp)
- **Mô tả:** Endpoint `GET /agent/v1/agent/commands` trả 405 Method Not Allowed
- **Tái hiện:** `curl -H "$H" $BASE/agent/v1/agent/commands`
- **Tác động:** Thấp — có thể là by design (chỉ hỗ trợ POST)

---

## QUAN SÁT KHÁC (Không phải bug)

| # | Quan sát | Ghi chú |
|---|----------|---------|
| OBS-01 | Public URL (app.welliam.codes) route tất cả path → Streamlit HTML | Reverse proxy (Caddy) chỉ proxy đến Streamlit, API không truy cập được từ bên ngoài |
| OBS-02 | Graph endpoint (`/graphs/entity`) trả 404 | Có trong OpenAPI spec nhưng chưa implement |
| OBS-03 | Report snapshots = 0 items | Hợp lý cho env staging mới |
| OBS-04 | Toàn bộ 33 vouchers = UNCLASSIFIED | Mock data chưa chạy classify pipeline |
| OBS-05 | Attachment runs liên tục failed (score=0.00) | "no confident match" — cần xem lại attachment matching logic |

---

## KHUYẾN NGHỊ

### Ưu tiên cao (trước release)
1. **BUG-QNA-01 + BUG-QNA-02:** Debug QnA pipeline — kiểm tra điều kiện trigger LLM, prompt template, và xử lý response rỗng từ LLM. Cần thêm fallback có ý nghĩa khi LLM trả rỗng.
2. **BUG-FEEDER-01:** Deploy feeder subprocess: cần sidecar container hoặc init script để khởi chạy `vn_invoice_feeder.py` cùng với agent-service pod.

### Ưu tiên trung bình
3. Implement endpoint `/graphs/entity` hoặc loại bỏ khỏi OpenAPI spec
4. Chạy pipeline classify cho mock vouchers để demo phân loại
5. Xem lại attachment matching logic (liên tục score=0.00)

### Ưu tiên thấp
6. Cân nhắc expose API qua reverse proxy cho client bên ngoài (nếu cần)
7. Thêm `GET /agent/commands` để list các lệnh có sẵn (hiện chỉ có POST)

---

## TÓM TẮT

Hệ thống **Accounting Agent Layer ERP AI Kế toán** hoạt động ổn định về mặt hạ tầng và API framework. Toàn bộ health check, validation, drilldown, bảo mật, và E2E data chain đều **PASS**. VN Data Stream + Command Center được deploy thành công về mặt UI/API, nhưng feeder process chưa active.

**Điểm yếu chính:** Module QnA kế toán — tính năng quan trọng nhất đối với người dùng cuối — chưa đạt yêu cầu. LLM path bị bypass hoặc trả rỗng. Cần ưu tiên fix trước khi release.

**Điểm mạnh:**
- Toàn bộ giao diện tiếng Việt (thông báo lỗi, label, timeline) ✅
- Fix DEV-01 (LLM URL masking) và DEV-04 (chống rò rỉ CoT) hoạt động đúng ✅
- Period validation chặt chẽ với thông báo rõ ràng ✅
- 10 goal chains kế toán Việt Nam ✅
- Authentication API key hoạt động ✅

---

*Report generated: 2026-02-10 | QA Blackbox Testing Session | No code/config modified*
